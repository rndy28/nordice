import { Button } from "@nordice/core";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { IconPlus } from "@tabler/icons";

<Meta title="Components/Button" component={Button} />

export const Template = (args) => <Button {...args} />;

# Button

Button is the primary component

<Canvas>
  <Story
    name="Primary"
    args={{
      children: "Primary",
      size: "lg",
      colorScheme: "primary",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary"
    args={{
      children: "Secondary",
      size: "lg",
      colorScheme: "secondary",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Warning"
    args={{
      children: "Warning",
      size: "lg",
      colorScheme: "warning",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Error"
    args={{
      children: "Error",
      size: "lg",
      colorScheme: "error",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="With Icon"
    args={{
      children: "With icon",
      size: "lg",
      colorScheme: "primary",
      icon: {
        element: <IconPlus size={18} className="inline" />,
        placement: "start",
      },
      className: "gap-2",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Disabled"
    args={{
      children: "Disabled",
      size: "lg",
      colorScheme: "primary",
      "aria-disabled": true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Rounded"
    args={{
      children: "Rounded",
      size: "lg",
      colorScheme: "primary",
      isRounded: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
